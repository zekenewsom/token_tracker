# Example configuration for the backend server
# Copy this file to `.env` and fill in your values

# Port the Express server will listen on
PORT=4000

# Path or connection string for the SQLite database
# Prisma expects a SQLite connection string, e.g. "file:./dev.db"
# For sqlite3 in the server you can also provide a simple path such as "./token_tracker.db"
DATABASE_URL=./token_tracker.db

# QuickNode RPC endpoint used for fetching transaction history
# Example: https://example-mainnet.solana.quiknode.pro/your-token/
QUICKNODE_ENDPOINT_URL=

# Helius RPC endpoint used for fetching token holders and other data
# Example: https://api.helius.xyz/?api-key=YOUR_KEY
HELIUS_RPC_URL=

# General Solana RPC URL. This can be the public mainnet RPC or your own node
# Example: https://api.mainnet-beta.solana.com
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
